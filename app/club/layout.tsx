import "./club.css";
export const metadata = {
  title: "LACACCINO Club",
  robots: { index: false, follow: false },
};
export default function ClubLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
